//POST	/api/shows/:id/like - Toggles the "Heart" on a drama. If it already exists, remove it; if not, add it.

//GET	/api/users/:id/likes	- Returns a list of all dramas a specific user has liked.

//POST	/api/reviews/:id/like	- "Like" another user's review.

//DELETE	/api/reviews/:id/like	- Remove your like from a review.
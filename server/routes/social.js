// POST /api/reviews - Adds a review to a show.

// GET /api/dramas/:id/reviews - Gets all reviews for a specific show.

// POST /api/watchlist - Adds a show to the user's list (Watching, Completed).

// GET /api/users/:id/watchlist - Displays a user's profile/list.

// POST /api/users/:id/follow - Follows another user.